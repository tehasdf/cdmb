# REST Service's Home directory
export REST_SERVICE_HOME="/opt/manager"

# REST Service's config files
export REST_SERVICE_CONFIG_PATH="/opt/manager/work/cloudify-rest.conf"
export MANAGER_REST_CONFIG_PATH="/opt/manager/work/cloudify-rest.conf"
export MANAGER_REST_SECURITY_CONFIG_PATH="/opt/manager/work/rest-security.conf"

# REST Service's virtualenv
export REST_SERVICE_VIRTUALENV="/opt/manager/env"
# Duplicate of the above. Need to check if unneeded.
export MANAGER_VIRTUALENV="/opt/manager/env"

# REST Service (and Gunicorn) Log Path
export REST_SERVICE_LOG_PATH="/var/log/cloudify/rest"

# REST Service port
export REST_PORT=8100
